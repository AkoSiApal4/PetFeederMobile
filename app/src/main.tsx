import { registerRootComponent } from "expo";
import App from "./App";

// Expo apps use registerRootComponent to make sure the environment is set up
// correctly whether running in Expo Go or in a native build.
registerRootComponent(App);
